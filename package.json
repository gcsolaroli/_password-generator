{
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "sjcl": "^1.0.8"
  },
  "scripts": {
    "build": "spago build --jobs 10 -- --codegen js,sourcemaps --verbose-errors",
    "develop-purs": "spago build --watch --clear-screen --jobs 10 -- --codegen js,sourcemaps --verbose-errors",
    "develop-app": "parcel --out-dir ./output/.dist ./src/html/index.html",
    "clean": "while read gitIgnorePattern; do rm -rf .${gitIgnorePattern}; done < .gitignore"
  },
  "engines": {
    "yarn": "^1.13.0"
  },
  "devDependencies": {
    "sass": "^1.21.0"
  }
}
